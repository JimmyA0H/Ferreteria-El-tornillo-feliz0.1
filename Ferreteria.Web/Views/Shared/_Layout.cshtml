
@{
    Layout = null;
    var titulo = ViewData["Title"] ?? "Ferretería El Tornillo Feliz";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <title>@titulo - Ferretería</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <style>
        body {
            background: linear-gradient(135deg, #f7f7f7, #e9f3ff);
            min-height: 100vh;
        }
        .navbar-brand {
            font-weight: 800;
            letter-spacing: 1px;
        }
        .hero {
            background: url("https://images.pexels.com/photos/4484078/pexels-photo-4484078.jpeg") center/cover;
            border-radius: 1rem;
            color: #fff;
            position: relative;
            overflow: hidden;
        }
        .hero::after {
            content: "";
            position: absolute;
            inset: 0;
            background: rgba(0,0,0,0.55);
        }
        .hero-content {
            position: relative;
            z-index: 2;
        }
        .card-ferreteria {
            border-radius: 1rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="/">🛠 Ferretería El Tornillo Feliz</a>
        </div>
    </nav>

    <main class="container my-4">
        @RenderBody()
    </main>

    @RenderSection("Scripts", required: false)

    <footer class="text-center text-muted mb-3">
        <small>&copy; @DateTime.Now.Year - Sistema de Gestión de Ferretería - POO C#</small>
    </footer>
</body>
</html>
